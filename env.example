# Copiază acest fișier ca ".env" (fără .example) și pune cheia ta reală aici.
# NU adăuga niciodată fișierul .env într-un depozit git public.

OPENAI_API_KEY=sk-...cheia-ta-reala-aici...

# Opțional — portul pe care rulează serverul (implicit 3000)
PORT=3000
